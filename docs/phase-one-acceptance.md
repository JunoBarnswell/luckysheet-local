# 第一阶段实施与验收记录

本记录对应 `codex/local-usable-foundation` 的阶段性实现。九项计划仍在推进；下列静态检查与局部行为证据不代表正式交付验收完成。

## 本轮实现边界

- React 浏览器通过 Java 同源 API、会话和 WebSocket 访问工作簿；H2 是持久化权威，IndexedDB 只记录待确认操作。
- 本地管理员初始化、账号登录和管理；本地认证与 OIDC 显式互斥。操作按客户端会话作用域去重，损坏和版本冲突返回错误。
- 原生资源读取验证长度与 checksum，中文文件名使用 UTF-8 `filename*`；大文件导入同时配置 multipart 和表单上限。
- 开始工具栏收紧分组宽度、统一按钮排布；窄窗口使用原有命令的折叠菜单。移除工具栏下方多余固定高度。
- 列标题与数据行分别占用 PaneMap 确定的区域；公式栏和网格编辑框按编辑会话的 surface 分配焦点，非活动编辑框不得抢焦点或修改光标。
- 去除 Electron 生产入口，构建统一 Node 24、JDK 21；前端产物进入 Maven 生成目录。提供 WinSW/NSIS 与备份恢复脚本。

## 2026-09-21 已验证

| 验证 | 结果与边界 |
| --- | --- |
| `frontend-react`: `npm run build` | 通过，包含 typecheck 和 Vite 构建；未执行前端测试，无 lint 脚本 |
| `npm run check:boundaries` | 通过，包含契约、命令注册、产物及验收矩阵静态检查；矩阵计数不代表案例已运行 |
| Java 21: `mvn -q test` | 26 个 suite、112 个测试，失败 0、错误 0、跳过 0 |
| 内嵌前端打包 | `mvn -q package -DskipTests` 通过；停止占用旧 JAR 的验收进程后重新打包，8082 改为仅由 JAR 内嵌资源提供页面，深链接与登录成功 |
| 开始工具栏 | 内置浏览器在 1186px 和 1480px 下无横向溢出；折叠菜单命令可访问，临时视口已恢复 |
| 原生 XLSX 导入 | 用户提供的真实文件导入服务端副本成功；原件 297336 字节、4059 行 × 23 列、93357 个源单元格 |
| 中文编辑与刷新 | 公式栏中文全选替换、Enter 提交、等待服务端保存确认、刷新恢复成功，版本从 0 变为 1 |
| 取消编辑 | Escape 恢复原值、版本保持 1；观察窗口内无提交请求，控制台无 warning/error |
| 服务重启 | 已导入工作簿与版本可恢复；持久化不是仅当前标签页内存状态 |
| 源文件保护 | SHA-256 前后相同：`dd5d7510103e7a7d92efb5acadf595eec740164943cd43693e842ae2d6b46eb6`；未将私有 corpus 纳入 Git |

## 尚未完成的正式验收

- 五个独立登录会话的权限撤销、竞争编辑、结构冲突、断线和重连矩阵。
- 真实中文 IME 组合输入、粘贴剪切、本人撤销重做、保护/合并/溢出拒绝的完整矩阵；自动输入中文不等于 IME 验收。
- 公式依赖、动态数组、跨表、过期计算丢弃、高频操作全链路与性能预算。
- XLSX 导出已从 UI 触发，但本轮未取得可核对的下载产物；导出→重导入→桌面 Excel 完整往返仍为 Blocked。机器有 Excel 安装，不能将其记为“Excel 不可用”。当前 corpus 不包含公式、图片、图表，相关覆盖仍缺失。
- 5 万行 × 20 列、百万非空单元格和 1 万公式的计时、帧耗时与峰值内存。
- NSIS 下载曾返回非安装包内容，被 checksum 校验拒绝；安装程序构建及首次安装、开机启动、升级、卸载保留数据和重装尚未通过。

## 迁移与回滚

客户端会话操作序号及快照版本升级在 Flyway 迁移边界执行；运行时拒绝未知版本，不保留双写或旧版读取作为恢复手段。损坏原生资源不返回空工作簿。IndexedDB 待确认操作必须先持久化，服务端确认并完成 checkpoint 后才清理。

升级前停止服务并使用备份脚本保留数据库、配置与相关数据。回滚整体恢复旧程序和升级前数据库备份；不得让旧程序读取新版数据库。正式部署与安装验收通过前，PR 保持 Draft。
