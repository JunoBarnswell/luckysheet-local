-- PostgreSQL V1 is a complete provider baseline. Keep the same version
-- boundary as the H2/MySQL migrations and make the catalog invariants
-- explicit for installations upgraded from an earlier provider baseline.
select 1;
